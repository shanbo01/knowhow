{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Local UI access for the recorder windows. Capture, credentials, files, and networking stay in Rust commands.",
  "windows": ["main", "hud"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-start-dragging"
  ]
}
